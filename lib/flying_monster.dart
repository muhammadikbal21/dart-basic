abstract class FlyingMonster { // class ini akan bertindak sebagai interface
  // interface adalah kumpulan method atau properti yang HARUS dimiliki oleh class yang mengimplement interface tersebut
  // perbedaan abstract dan interface yaitu:
  // class (abstract) hanya bisa extends 1 parent saja
  // implement (interface) bisa banyak dari class yang mengimplementasikan interface tersebut
  String fly();
}